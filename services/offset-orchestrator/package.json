{
  "name": "@caas/offset-orchestrator",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist || true",
    "build": "pnpm run clean && tsc -p tsconfig.json",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@caas/shared-metrics": "workspace:*",
    "kafkajs": "^2.2.4",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^20.11.0",
    "@types/pg": "^8.10.2"
  }
}
